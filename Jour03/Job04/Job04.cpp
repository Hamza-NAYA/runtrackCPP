#include <iostream>
#include <cstring>

using namespace std;

int main(void) {
    char arr[] = "Vive la plateforme !";

    int len = strlen(arr);

    cout << arr << endl;
    cout << "La taille est " << len << endl;

    return 0;
}
